# ChaoticNet 论文编译说明

## 环境要求

需要 **TeX Live 2024+**（含 ctex 宏包和 biblatex/biber）。Mac 上推荐：

```bash
brew install --cask mactex          # 完整版（推荐，~5GB）
# 或
brew install --cask basictex        # 精简版，需要手动补包
```

精简版补包：

```bash
sudo tlmgr update --self
sudo tlmgr install ctex biblatex biber physics cleveref booktabs \
                   subcaption algorithm algorithmicx
```

## 编译命令

中文论文必须用 `xelatex`，不能用 `pdflatex`：

```bash
cd paper
export TEXINPUTS="./texstyles/gb7714//:"
xelatex main.tex
biber main
xelatex main.tex
xelatex main.tex                    # 跑两遍交叉引用稳定
```

或者用 `latexmk` 一键搞定：

```bash
TEXINPUTS="./texstyles/gb7714//:" latexmk -xelatex main.tex
```

## 图片路径

`main.tex` 已配置 `\graphicspath` 包含：

- `paper/figures/`                                       本目录的图
- `data/sim/figures/v1_baseline_2026-06-11/`            仿真基线图
- `data/rc/figures/v1_baseline_2026-06-11/`             ESN 基线图
- `data/pinn/figures/v1_baseline_2026-06-11/`           PINN 基线图

所以在 `\includegraphics{fig_energy_sweep.png}` 时不需要写路径。

## 章节进度（W4 起填）

| 章节 | 状态 | 负责人 |
|---|---|---|
| Abstract | 占位完成 | 队长 |
| 1. 引言 | 占位完成 | 队长 + 文献综述 E |
| 2. 物理模型 | 公式骨架 OK，待展开 $\bm{M}, \bm{b}$ | 队长 |
| 3. 数值仿真 | 占位 + 数字已填 | 队长 |
| 4. 实验装置 | 占位 + 参数已填 | 实验员 |
| 5. 视觉系统 | 占位 | 视觉工程师 |
| 6. AI 模型 | PINN 损失函数已写完 | 机器学习 D |
| 7. 结果分析 | v1 数据表已填 | 机器学习 D + 队长 |
| 8. 讨论与展望 | 占位 | 全员 |

所有 `\textcolor{red}{[TODO: ...]}` 是待填空，红字编译出来很显眼。

## 文献

`references.bib` 采用 GB/T 7714—2015 顺序编码制输出；样式文件已随项目放在
`texstyles/gb7714/`，因此编译时需保留上面的 `TEXINPUTS` 环境变量。文献库已收核心条目：

- 双摆动力学：Shinbrot 1992
- ESN：Jaeger 2004、Pathak 2018
- PINN：Raissi 2019、Greydanus 2019（HNN）、Cranmer 2020（LNN）
- 混沌教材：Ott 2002

W4 起补充摄影测量学和实测数据相关文献。
