# 第 1 项结果：ESN 闭环多消耗一步的修正与影响面

生成时间 2026-09-05 01:07:17；脚本 `tools/esn_closedloop_fix.py`

## 自证门槛

| 门槛 | 内容 | 结果 |
|---|---|---|
| G1-A | `legacy(x_f,u0,n) == readout_first(step(x_f,u0),u0,n)` 逐位相同 | PASS，maxdiff = 0.000e+00 |
| G1-B | 同参数裸换必须**不**等价（反向禁令取证） | PASS，真实侧 maxdiff = 3.332e+00，帧0误差中位 0.132° → 7.054° |
| G2 | legacy per-run own_chaos 中位复现 canon_multirun/summary.csv | 0.4041666666666667 [0.239583, 0.458333]（目标 0.4041666666666667） |
| G3 | legacy 逐 trial 跨 run 中位复现冻结 four_way `esn` 列 | maxdiff = 2.220e-16 |

## 仿真内表（own_chaos 口径，10 seed，可直接替换 tab:horizon-compare 的仿真内块）

| 模型 | 中位 (s) | IQR | /τL | ×ESN(旧) | ×ESN(修正) |
|---|---|---|---|---|---|
| ESN 旧（错误路径） | 0.404 | [0.240, 0.458] | 0.53 | 1.00× | — |
| **ESN 修正** | **0.696** | [0.551, 0.700] | 0.91 | — | 1.00× |
| PINN（λ=0.1）[冻结] | 0.875 | — | 1.15 | 2.16× | **1.26×** |
| 纯数据 MLP（λ=0）[冻结] | 1.550 | — | 2.03 | 3.84× | **2.23×** |
| Hybrid[冻结] | 2.850 | — | 3.74 | 7.05× | **4.10×** |

每 run 混沌子集大小：旧 30–38 条，修正后 25–33 条。

**own_chaos 口径偏保守，必须点破**：ESN 变强后更多 trial 视界 ≥9.9s 被踢出各自的混沌子集，而被踢出的恰恰是修正版表现最好的那些 trial。因此 0.696s 是**下界**。同时给出消除子集漂移的交集口径（只保留两条路径都判为混沌的 trial）：

| 口径 | ESN 旧 | ESN 修正 | 每 run N | PINN/ESN | MLP/ESN | Hybrid/ESN |
|---|---|---|---|---|---|---|
| own_chaos（各自子集） | 0.404 | 0.696 | 30–38 → 25–33 | 2.16× → 1.26× | 3.84× → 2.23× | 7.05× → 4.10× |
| 交集（同一子集） | 0.302 | 0.696 | 25–33 | 2.90× → 1.26× | 5.13× → 2.23× | 9.43× → 4.10× |

> 交集口径的 PINN/MLP/Hybrid 中位仍取冻结的 per-run own_chaos 值（未按交集重算），所以那三个倍数只是量级参考，不能当正式数字引用；正式表用 own_chaos 行。

## 四方固定子集（逐 trial 跨 run 中位；PINN/MLP/Hybrid 复用冻结列）

| 口径 | N(三方) | ESN | PINN | ×ESN | MLP | ×ESN | Hybrid | ×ESN |
|---|---|---|---|---|---|---|---|---|
| legacy(旧/错误) | 32 | 0.294 | 0.506 | 1.72× | 0.881 | 3.00× | 2.167 | 7.38× |
| fixed(修正) | 28 | 0.558 | 0.429 | 0.77× | 0.765 | 1.37× | 1.794 | 3.21× |
| 同一子集(legacy定义) | 32 | 0.294 | 0.506 | 1.72× | 0.881 | 3.00× | 2.167 | 7.38× |
| 同一子集(legacy定义)/ESN_fixed | 32 | 0.733 | 0.506 | 0.69× | 0.881 | 1.20× | 2.167 | 2.95× |

## 真实侧（完全不受影响）

真实零样本 ESN 0.167s / MLP 0.358s / PINN 0.417s / Hybrid 0.450s 四个数字**一个不动**，因为真实侧调用（`rc/real_validation_multi.py:104`）属正确族。见 `callsites.md`。

## 影响面

见同目录 `callsites.md`：错误族 6 个调用点、正确族 6 个（其中 2 个是今晚新增的 `tools/esn_grid_real.py`）。`tools/threshold_robustness.py:179` 的仿真侧 ESN 曲线同样受此影响，其真实侧不受影响。

